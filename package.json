{
  "name": "tournmate-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "deploy:dev": "npm run build -- --mode development && firebase deploy --only hosting -P dev",
    "deploy:prod": "npm run build && firebase deploy --only hosting -P prod"
  },
  "dependencies": {
    "firebase": "^11.0.0",
    "qrcode.react": "^4.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
